# 1C OData Metadata

This folder stores verified 1C OData metadata snapshots.

Required target file:

```text
standard_odata_metadata.xml
```

Current status:

```text
BLOCKED: current metadata contains no business entities.
```

Do not create fake metadata. Save the real `$metadata` response only after required entities are published in 1C Client.
