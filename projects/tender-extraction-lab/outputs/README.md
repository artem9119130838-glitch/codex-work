# outputs (compat)

This folder is kept for compatibility with older workflows.

Canonical locations in this lab:

- Reports: `reports/`
- Any large generated artifacts should go to `reports/_local/` (ignored by git).

