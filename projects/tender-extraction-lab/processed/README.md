# processed (compat)

This folder is kept for compatibility with older workflows.

Canonical locations in this lab:

- Derived/trace artifacts (ignored by git by default): `data/legacy/`
- Reports: `reports/`

