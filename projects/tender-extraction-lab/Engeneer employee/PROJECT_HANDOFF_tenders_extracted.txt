tender-extraction-lab тАФ PROJECT_HANDOFFProject PurposeBuild a measurable tender-lot extraction pipeline that reliably produces:purchase_subject (what is being purchased)payment_terms (PAY)delivery_terms (DEL)provenance (which file/page/snippet supported each field) The pipeline must work on messy lot folders (PDF/DOCX/XLSX) and must be suitable for later automation (Google Drive + n8n + Bitrix). Current ProblemOld parser/chunker/extractor plateaued at ~60тАУ70% match vs human gold. The issue is not only LLM quality. The main failure mode is that the system often fails to provide correct candidate chunks:missing the true clause (no recall),selecting boilerplate/legal clauses (noise),failing to jump to appendices/specs where the real terms live. Therefore the new project must be candidate-recall first and LLM second. Human Meaning of the ProjectThis is not тАЬmake an LLM answer better.тАЭ It is тАЬgive the model the right evidence.тАЭThe business user needs accurate, auditable terms with clear sources, not just text completion. Why the Old Parser Failed (Root Causes)1. Candidate quality/recall was not measured separatelyтАЬOutput matchтАЭ mixed: text inventory + chunk selection + LLM extraction + normalization.2. Noise dominateslegal/boilerplate clauses («Не несет ответственности...», guarantees, invoices, penalties) win scoring.3. Missing evidence jumpscorrect terms often live in appendices/specs; the pipeline didnтАЩt consistently follow references.4. Multi-layer edits hid regressionschanges across chunking + scoring + extraction at once made it hard to attribute outcomes. Migration Status (from MIGRATION_MANIFEST.csv)The new project should treat the old repo as an archive plus a few utility scripts. Canonical Files (new project тАЬsource of truthтАЭ)These should be considered canonical starting points (not legacy):data/gold/19-05/Сводка_лотов_верная 19-05.xlsx (human gold benchmark)scripts/eval/eval_min_context_vs_gold_19_05.py (regression harness vs gold; keep/extend)scripts/batch/run_matrix_onlylot.py (variant matrix runner for one-lot evaluation) Legacy Files (reference only тАФ do not turn into production)Everything routed under legacy/old_project/... is reference only, including:parsers: tender_lot_parser_v7.py, tender_min_context_extractor_v1.py, chunkers/buildersprompts/reports/logs from prior model runs and analysisWindows batch wrappers Rule: legacy code is for reading/learning, not for тАЬfixing and shipping.тАЭ DiscardUnrelated assets (e.g., images) should not migrate. New Project Structure (Target)Recommended high-level layout in tender-extraction-lab:data/gold/ (human-validated datasets, versioned by date)raw_samples/ (small curated lot folders used for tests; never full dumps by default)scripts/gold/ (loaders, schema validation, column mapping)inventory/ (text inventory and file metadata)candidates/ (candidate generation and recall evaluation)eval/ (recall + regression reports)batch/ (batch builders: JSONL/MD/CSV generators)llm/ (LLM extraction runners/prompts; must be gated by recall metrics)validate/ (output validators and consistency checks)legacy/old_project/ (frozen archive; do not refactor) Pipeline Phases (Must Be Implemented and Measured)Do not call an LLM before Phase 4 is measurable. 1. gold loader2. text inventory3. candidate finder4. candidate recall evaluator5. batch builder6. LLM extractor7. output validator8. regression report Gold Spreadsheet Column Meaning (Contract)Gold XLSX columns (must be supported by the loader):B = parser_purchase_subjectC = human_purchase_subjectD = human_purchase_subject_sourceG = advanceH = parser_payment_termsI = human_payment_termsJ = human_payment_terms_sourceK = parser_delivery_termsL = human_delivery_termsM = human_delivery_terms_sourceN = local_tender_folderQ = source_zipR = no_participation_reasonS = no_participation_source What Is Already Available (per manifest)A human gold XLSX for 19-05 with labels and sources.A working gold-eval harness that can be evolved into proper recall@k + regression reporting.A one-lot variant matrix runner pattern for тАЬ1 change тЖТ 1 lot тЖТ record outcomeтАЭ. What Is Missing (Must Be Built in the New Lab)1. Gold schema + column mapping as code2. Text inventorydeterministic extraction per file type with metadata (file kind, size, page count for PDF, warnings, text length, etc.)3. Candidate finder with recall@kproduce candidates for PAY/DEL/SUBJECT with provenancecompute recall metrics vs gold sources before using LLM4. Batch builderbuild minimal LLM input batches from candidates (bounded size + provenance)5. Output validatorchecks: empty/too long, wrong-type clauses (invoice/penalty instead of PAY/DEL), missing provenance What Must Not Be Donelegacy code is reference only; do not rewrite the parser.do not call LLM before candidate recall is measured.do not edit multiple pipeline layers in one task.do not claim completion without regression. Acceptance Gates (Non-Negotiable)1. Reproducibility gate: same inputs тЖТ same outputs.2. Recall gate (pre-LLM): publish recall@k for PAY/DEL/SUBJECT candidates vs gold.3. Regression gate: every change must run the relevant regression suite and record deltas.4. Provenance gate: outputs must include sources (file; page where possible; snippet). Next 3 Implementation Tasks (one layer per task)1. Gold loader module (XLSX тЖТ typed records + column validation).2. Text inventory (lot folder тЖТ per-file extracted text + metadata JSON).3. Candidate recall evaluator baseline (regex/heuristics тЖТ recall@k report; no LLM calls). What a New Codex Chat Must Read First1. PROJECT_HANDOFF.md2. MIGRATION_MANIFEST.csv3. data/gold/19-05/Сводка_лотов_верная 19-05.xlsx4. scripts/eval/eval_min_context_vs_gold_19_05.py Subagents for Medium/Large TasksSplit work by pipeline layer:Gold/SchemaText InventoryCandidates/RecallBatch/Prompt (no model calls)LLM Extraction (only after recall gate)Validation/Regression 
