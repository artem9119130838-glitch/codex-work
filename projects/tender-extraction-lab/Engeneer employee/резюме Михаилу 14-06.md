### HR-оценка инженера (Михаил Денисов)

* **Общая характеристика:** Михаил — сильный Middle+/Senior разработчик с отличными архитектурными навыками и хорошим продуктовым мышлением. Он не просто бездумно пишет код по ТЗ, а анализирует ограничения инфраструктуры (VPS) и предлагает грамотные оптимизации. 
* **Плюсы:**
  1. *Быстрая адаптивность и решение проблем:* При угрозе OOM (нехватки памяти) на сервере оперативно отказался от локальной модели `sentence-transformers` в пользу облачного API.
  2. *Высокое качество кода:* Архитектура FastAPI приложения написана чисто, модульно и разделена по слоям (роутеры, схемы, сервисы).
  3. *Интеграционные навыки:* Использование пакетной векторизации (batching по 100 чанков) для обхода лимитов и туннелирования `bore` для n8n показывает хорошую инженерную смекалку.
* **Минусы:**
  1. *Изолированная разработка (Главный минус):* Михаил работал локально на своем Macbook и залил код в свой личный репозиторий в обход Antigravity. Это лишает проект общей истории изменений и мешает совместной работе в одном воркспейсе.
  2. *Безопасность данных:* Захардкодил плейсхолдеры ключей API прямо в URL-адреса n8n-нод.
  3. *Фокус внимания:* Его нужно направлять на приоритетные задачи (например, переключение на парсер тендеров — это ваша идея, которую он успешно реализовал, но изначально он двигался в сторону других ML-моделей).
* **Что с него потребовать и план работы:**
  1. Внедрить жесткий регламент работы в общем воркспейсе через Antigravity (составление планов реализации, верификация коммитов).
  2. Перенести сервис на продакшн-сервер и настроить безопасное хранение API-ключей в n8n Credentials.
  3. Начать интеграцию извлеченных данных с Битрикс24.

---

### Текст сообщения для Михаила (два блока)

Скопируйте и отправьте Михаилу следующий текст (обращение на «вы», тактично, но с четкими границами):

#### Блок 1: Обратная связь по проделанной работе и архитектуре
> **Михаил, добрый день!**
> 
> Посмотрел ваш код и архитектуру RAG-системы в репозитории `tender-rag-api`. Хочу поблагодарить за проделанную работу — решение получилось очень изящным и технологичным! 
> 
> Особенно выделю несколько отличных решений:
> 1. **Оптимизация ресурсов VPS:** Перенос генерации эмбеддингов с локальной модели `sentence-transformers` на облачный Gemini Embeddings API — это правильный шаг. Он полностью решает проблему дефицита ОЗУ на сервере.
> 2. **Пакетная векторизация:** Реализация пакетной отправки (batching) по 100 чанков в Python-коде написана грамотно и страхует нас от таймаутов и ошибок 429.
> 3. **Модульность:** Разделение логики на FastAPI-микросервис (парсинг и нарезка), Postgres (хранение векторов через pgvector) и n8n (оркестрация и генерация ответов LLM) обеспечивает отличную масштабируемость.
> 
> Также хочу уточнить по поводу переключения на парсер тендеров — это приоритетное направление, которое мы с вами утвердили. Рад, что вы быстро подхватили эту задачу и перестроили под неё систему.
> 
> При этом у меня есть пара важных замечаний по безопасности и процессу разработки:
> * В файлах n8n-воркфлоу API-ключи жестко прописаны прямо в URL-адресах запросов (`YOUR_GEMINI_API_KEY`). Нам нужно вынести их в безопасные Credentials внутри самого n8n.
> * Самое главное: вы вели разработку локально и запушили код в сторонний репозиторий. Чтобы не терять историю изменений, планов и проверок, нам критически важно вести проект в едином управляющем контуре Antigravity. Ниже я подготовил для вас простую инструкцию, как мы будем взаимодействовать дальше.

#### Блок 2: Инструкция по работе с Antigravity и регламент отчетности
> Поскольку Antigravity является нашим управляющим контуром воркспейса, все дальнейшие задачи мы ведем по следующему регламенту:
> 
> **1. Перед началом любой новой задачи:**
> * Создайте в корне проекта файл `implementation_plan.md` (план реализации).
> * Опишите в нем архитектуру изменений, новые файлы и зависимости, а также план верификации.
> * Отправьте план мне на согласование.
> 
> **2. В процессе разработки:**
> * Фиксируйте ключевые вехи в файле `PROGRESS.md` в корне репозитория.
> 
> **3. Перед сдачей задачи и пушем в Git:**
> * Запустите локальный чеклист самопроверки из файла `SELF_CHECK.md`.
> * Убедитесь, что все файлы с кириллицей сохранены в кодировке **UTF-8 с BOM** (это стандарт для нашего воркспейса, чтобы избежать проблем с кодировкой на сервере).
> * Проведите верификацию кода.
> 
> **Регламент отчетности и согласования:**
> * **Что согласовывать:** Любые изменения в структуре базы данных, появление новых внешних зависимостей (библиотек) и изменения в архитектуре интеграции с n8n / 1С / Битрикс24.
> * **Когда согласовывать:** Планы реализации — строго *до* написания кода. Демонстрацию готового функционала — по факту выполнения задачи (вместе с отчетом о прохождении `SELF_CHECK.md`).
> * **Как:** Писать в наш рабочий чат со ссылкой на коммит в Git и обновленный файл `PROGRESS.md`.
> 
> Давайте примем эти правила за стандарт. Это позволит нам прозрачно видеть историю проекта в git-дереве и избежать дублирования работы. 
> 
> Следующим шагом предлагаю заняться переносом `Tender RAG API` на продакшн VPS, исправлением безопасности ключей в n8n и началом интеграции результатов с Битрикс24. Что скажете?