# incoming (compat)

This folder is kept for compatibility with older workflows.

Canonical locations in this lab:

- Gold benchmarks: `data/gold/`
- Local-only tender samples (ignored by git): `data/raw_sample/`
- Reports: `reports/`

