# Global Progress (Personal)

Дата обновления: 2026-07-18

## 1. Текущий статус проектов (Current State)

* **Единый центр управления:** `C:\Codex_Personal` остается единым центром управления Codex KB и личным Git-репозиторием.
* **Принцип Single Source of Truth:** Все текстовые базы знаний в Markdown являются первоисточником истины. Производные базы данных (например, `marketing_db.knowledge_base` на Postgres) обновляются строго по ним.
* **Стабилизация Ingestion (Phase 2):** Процесс импорта данных из 1С в PostgreSQL (`onec_owners`, `onec_contacts`) завершен и работает стабильно в линейном режиме, исключающем гонки данных.

---

## 2. Ключевые достижения (Done)

* **Сетевая безопасность и правила ИИ:** Настроена защита от утечек Git (pre-push хук наAllowed Remotes), изоляция личного/общего контуров и жесткие лимиты на частоту SSH-подключений (Rate Limiting) во избежание банов IP провайдером.
* **Оптимизация и аудит VPS (Июль 2026):**
  - **Дисковое пространство:** Удалено более 82 ГБ устаревших логов и бэкапов. Неактивные БД 1С перенесены на диск `/Storage` с помощью PostgreSQL Tablespaces, освободив 10.6 ГБ на системном SSD `/` (доступно 28 ГБ).
  - **Оперативная память:** Отключены неиспользуемые GUI-службы (`xrdp`, `lightdm`), оптимизирована работа Swap. Свободно ~5.7 ГБ оперативной памяти.
  - **Безопасность:** Отключен парольный вход для root, авторизация настроена строго по приватному ключу `id_ed25519_wlisses`.
* **Интеграция RAG (Микросервис):** 
  - Развернута база данных `pgvector` в контейнере Docker на VPS (порт 5433).
  - Настроен Gemini Embeddings API (отказ от тяжелых локальных моделей векторизации для защиты ОЗУ от OOM-ошибок).
  - Alembic-миграции для схемы matching/RAG применены.

---

## 3. Активные задачи и приоритеты (Next)

1. **Исправление отчета «KPI Менеджеров» (1С:УНФ):**
   Замена запроса набора данных «Факт» в СКД отчета с использованием регистра `ФинансовыйРезультат` и верификация расчетов прибыли.
2. **Пилотный запуск n8n_email_ai (Phase 3):**
   Включение автоматического intake почты (IMAP), дедупликация и сопоставление (matching) входящих писем с базой `onec_owners` / `onec_contacts`.
3. **Проверка прав и доступов Чулпан:**
   Запуск скрипта `check_drive_access.py` для аудита прав сотрудника на Google Диске руководителя.
4. **Контроль инфраструктуры:**
   Сохранение жесткого разделения на два рабочих чата: `ingestion` (БД/1С) и `RAG/emails` (почта/шаблоны).
